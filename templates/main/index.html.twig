{% extends 'base.html.twig' %}

{% block title %}Hello MainController!
{% endblock %}

{% block body %}
	<div class="pokemonSearch">
		{{form(form)}}
	</div>

	<div class="pokemonGrid">
		{% for pokemon in pokemons%}
			<div>
				<a class='pokemon' href="{{ path('pokemon', {name: pokemon.name}) }}">
					<div><img src="{{pokemon.sprite}}" alt="" width='200px'></div>

					<div>{{pokemon.name}}</div>
					<div>
						{% for type in pokemon.types %}
							<div class="type {{ type }}">{{ type }}</div>
						{% endfor %}
					</div>
				</a>
			</div>
		{% endfor %}
	</div>

	<div class="mg-y-20 text-center">
		<a href="{{ path('index', {limit: limit + 10}) }}" class="btn-primary">Charger d'autres Pokémon</a>
	</div>

	<footer></footer>
{% endblock %}
